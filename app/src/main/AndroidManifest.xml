<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.membership_score">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".app.AppApplication"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:resizeableActivity="false"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <activity android:name=".ui.common.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.account.activity.RegisterActivity"/>
        <activity android:name=".ui.account.activity.LoginActivity"/>
        <activity android:name=".ui.account.activity.MemberModifyPWActivity"/>
        <activity android:name=".ui.account.activity.MemberForGetPWActivity"/>
        <activity android:name=".ui.common.activity.MainActivity"/>
        <activity android:name=".ui.account.activity.AddMemberActivity"/>
        <activity android:name=".ui.home.activity.MemberDetailsActivity"/>
        <activity android:name=".ui.home.activity.MemberShipAddActivity"/>
        <activity android:name=".ui.home.activity.MemberShipRecordActivity"/>
    </application>

</manifest>